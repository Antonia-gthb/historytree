import { useState, useEffect } from 'react';
import {
  color as handleColor,
  hsvaToHex,
} from '@uiw/color-convert';

import Chrome from '@uiw/react-color-chrome';
import { GithubPlacement } from '@uiw/react-color-github';

//interface ColorScaleProps {
    //setEventColor: (color: string) => void; // Callback für das Setzen der Eventfarbe
  //}

//const ColorScale: React.FC<ColorScaleProps> = ({ setEventColor }) => {
    //const [hsva, setHsva] = useState({ h: 0, s: 25.71, v: 82.35, a: 0.32 });
  //const hex = hsvaToHex(hsva)

  //const handleColorChange = (color: any) => {
    //setHsva(color.hsva); // Setze die HSVA-Farbe
    //setEventColor(hex);   // Setze die Hex-Farbe (die an die Graph-Komponente weitergegeben wird)
  //};
  //return (
    //<>
      //<Chrome
    //    color={hsva}
    //    style={{ marginTop: 10, width: 140 }}
     //   placement={GithubPlacement.TopRight}
      //  showEyeDropper={false}
      //  showColorPreview={false}
     //   showEditableInput={false}
    //    onChange={handleColorChange}
     // />
   // </>
 // );
//}
//export default ColorScale;







