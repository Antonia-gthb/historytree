import rawdata from '@/app/tonis_orders_tree_2.json';


//const parsedData = JSON.stringify(rawdata);

export default rawdata

//function ShowData () {
  //console.log(parsedData); // Überprüfen, ob es klappt
  //return <div> parsedData={parsedData}</div>;
  //return <pre>{JSON.stringify(parsedData, null, 2)}</pre>;
//}